# MongoDB connection string used by the Express booking server
# Replace the value below with your MongoDB Compass URI before starting the server.
MONGODB_URI=mongodb://localhost:27017/kurumbapatti-zoo

# Optional: override the port the Express server listens on
# PORT=3000
